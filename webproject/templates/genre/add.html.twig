{% extends "base.html.twig" %}

{% block body %}
<div class="container col-md-6 mt-3 mb-3">
   <h2 class="text-center">ADD NEW GENRE</h2>
   <div class="col md-6"></div>
      {{ form_start (genreForm) }}
         <div class="form-group">
            {{ form_label (genreForm.name)}}
            {{ form_widget (genreForm.name ,
               {
                  'attr' : {'class' : 'form-control'}
               })
            }}
         </div>
         <div class="form-group">
            {{ form_label (genreForm.description)}}
            {{ form_widget (genreForm.description ,
               {
                  'attr' : {'class' : 'form-control'}
               })
            }}
         </div>
      <div class="form-group text-center mt-3">
         <input class="btn btn-primary" type="submit" value="Add" />
      </div>
      {{ form_end (genreForm)}}
   </div>
</div>
{% endblock %}